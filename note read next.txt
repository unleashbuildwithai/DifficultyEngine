CONTEXT HANDOFF — read this first in the new chat window
==========================================================

PROJECT: DifficultyEngine (Paper/Spigot 1.21 Minecraft plugin)
Location: c:\Users\Owner\Desktop\123
Repo: https://github.com/unleashbuildwithai/DifficultyEngine.git

RECENTLY COMPLETED (verified working, compiles clean):
-------------------------------------------------------
1. Magic system rewrite (3k+ line ElementalProcManager overhaul):
   - Replaced old combo-chain elemental system with 4 independent procs:
     Burn / Wet / Muddy / Chilled→Frozen
   - Stripped combo-chain logic from ComboFavoritesManager + FavoritesGUI
   - Simplified handleFireHit/handleWaterHit/handleEarthHit/handleAirHit
     (earth's old trap/suffocate system removed, added pickaxe-escape)
   - Wired Burn-extinguish via Downpour, Frozen-melt via Fire Staff
   - Rewrote SpellBookManager pages to describe procs instead of combos
   - Wired Main.java (ElementalProcManager.setItemFactory etc.)

2. Fixed a bunch of real compile bugs found while doing a full javac
   verification pass (build_check.bat / cp_list.txt):
   - cp_list.txt had trailing whitespace on every line breaking classpath
     parsing in the batch for-loop (fixed by trimming)
   - 18 files used deprecated/removed `Attribute.GENERIC_*` constants
     (e.g. GENERIC_MAX_HEALTH) that don't exist in current Paper API —
     renamed to MAX_HEALTH / ATTACK_DAMAGE / MOVEMENT_SPEED /
     FOLLOW_RANGE / SCALE (no GENERIC_ prefix)
   - NpcQuestRegistry.java had a bad `Material.CHAIN` reference — fixed
     to IRON_CHAIN
   - My own automated fix-up script accidentally added UTF-8 BOM bytes
     to those 18 files — had to strip BOMs afterward
   - Full build now compiles with EXIT=0, no errors (verified via
     direct PowerShell javac invocation against full Paper API classpath)

3. Confirmed NpcQuestRegistry.java holds all 300 quest defs in one
   place (mainQuests() 1-150, secretQuests() 151-300, byId() lookup).

4. Confirmed /npcwipe (alias /killallnpc) already exists
   (NpcWipeCommand.java) — wipes all quest/VIP NPC villagers +
   their persisted position records. Admin perm:
   difficultyengine.cape.admin

CURRENTLY IN PROGRESS / NOT YET DONE — PICK UP HERE:
------------------------------------------------------
User reported 3 related bugs (diagnosed but NOT yet fixed):

  BUG A — Custom boss models & cape textures don't show in-game at all.
    ROOT CAUSE (confirmed): server.properties has resource-pack=
    completely BLANK. The RP zip (DifficultyEngine-RP.zip) exists and
    was built correctly by gen_resourcepack.py, but the server never
    actually pushes it to clients because the URL/SHA1 fields are empty.
    FIX NEEDED:
      1. Host DifficultyEngine-RP.zip somewhere with a direct download
         URL (GitHub Releases on the repo above is easiest, or
         mc-packs.net, or any web host)
      2. Compute/obtain its SHA1 (gen_resourcepack.py already prints
         this when run — rerun it or sha1sum the zip)
      3. Set in server.properties:
           resource-pack=<URL>
           resource-pack-sha1=<hash>
           resource-pack-required=false
    STATUS: blocked on user telling us where they'll host the zip file.
    NEXT STEP: ask user for hosting choice, then edit server.properties.

  BUG B — Boss room schematics ("ongs") / arenas don't rebuild when a
    boss spawner is activated, even though user confirmed WorldEdit
    IS installed on their server.
    SUSPECTED CAUSE: CrimsonBossManager.rebuildArena() (around line
    1006-1078 in src/main/java/com/yourname/difficulty/boss/
    CrimsonBossManager.java) calls:
        finalPlayer.performCommand("schematic load " + finalSchem);
        finalPlayer.performCommand("/paste");
    This command syntax may not match the installed WorldEdit version
    (should likely be "//schem load <name>" and "//paste" with the
    double-slash prefix that WorldEdit actually registers, or the
    command may need world-edit specific flags). No error handling/
    logging exists to confirm success or failure either — if it's
    silently failing we'd never know from server logs currently.
    NEXT STEPS (not yet done):
      1. Verify actual WorldEdit command names/aliases registered
         (check WorldEdit version, permissions the player executing
         has — note code does temporarily setOp(true) around the
         performCommand calls, so permission shouldn't be the issue)
      2. Add success/failure logging around the schematic load+paste
         calls so failures are visible in console
      3. Fix command syntax if wrong
      4. Consider replacing performCommand-based approach with
         WorldEdit's actual Java API (if the jar is on the classpath)
         for more reliable integration instead of relying on chat
         command parsing

  BUG C — Crying dome particles only show purple, never blue/green.
    ROOT CAUSE (confirmed, by design not a bug per se): purple comes
    from the *native* vanilla crying_obsidian block mechanic (no
    plugin/schematic needed). Blue + green particles come from 3
    command blocks in crying_dome_particles.schem
    (gen_crying_dome_particles.py) that must be manually pasted at
    the EXACT dome center via WorldEdit — this manual step was
    likely never performed.
    NEXT STEPS (not yet done, pick one):
      OPTION 1: Just walk the user through manually pasting
        crying_dome_particles.schem via //schem load + //paste at
        the dome center (quick, no code changes)
      OPTION 2: Auto-paste this schematic from Java automatically
        whenever the crying dome area is set up / boss room rebuilds,
        removing the manual step permanently (more code, more robust)
      Ask user which they'd prefer once resumed.

OTHER NOTES:
------------
- User is going to try downloading "Nexo" (a resourcepack/custom-item
  management plugin, successor-style to ItemsAdder) — was told Nexo
  would NOT fix any of bugs A/B/C above since those are unrelated to
  custom-item authoring tooling; it only affects how custom item
  models/textures are AUTHORED, not whether the resourcepack gets
  served to the client or whether WorldEdit schematics paste
  correctly. If user still wants to integrate Nexo for the custom
  item side later, that would be a separate larger migration task
  (re-registering existing custom items through Nexo's config format)
  — not urgent, purely a nice-to-have for authoring convenience.

- Temp files from the compile-check work (out_check*, compile_check*.txt,
  cp_oneline.txt, TestCompile.java, test_marker.txt) were already
  cleaned up. cp_list.txt was permanently fixed in place (trailing
  whitespace stripped) so future build_check.bat runs should now give
  a TRUSTWORTHY EXITCODE — but note build_check.bat's own inline
  batch scripting for %errorlevel% capture inside a for-loop is
  unreliable/misleading (always prints EXITCODE=0 regardless of real
  result due to delayed expansion timing) — prefer running javac
  directly via PowerShell (see the working one-liner pattern used
  during the compile-check session, or just build_check.bat's actual
  compile_check.txt output content itself, not the "EXITCODE=" line
  it prints, to verify success/failure) if verifying compile status
  again in the new chat window.

WHAT TO DO WHEN RESUMING:
--------------------------
1. Ask user which resourcepack hosting method they picked (or offer to
   set up a GitHub Release using the git remote already configured)
2. Investigate & fix CrimsonBossManager.rebuildArena() WorldEdit
   command syntax + add logging
3. Handle the crying dome particle schematic (manual walkthrough vs
   auto-paste in code — ask user preference)
4. Re-run a full javac compile-verify after any code changes (use
   build_check.bat but check compile_check.txt content directly,
   don't trust the printed EXITCODE line)
